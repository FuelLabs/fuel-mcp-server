# Example: sway_sources/sway-applications/oracle/oracle-contract/src/events.sw

```sway
library;

/// Event for when the price of an asset is updated.
pub struct PriceUpdateEvent {
    /// Updated price.
    pub price: u64,
}

```
