# Example: docs/reference/src/code/language/program-types/scripts/simple_script/src/main.sw

```sway
script;

// All scripts require a main function. The return type is optional.
fn main(amount: u64) -> bool {
    true
}

```
