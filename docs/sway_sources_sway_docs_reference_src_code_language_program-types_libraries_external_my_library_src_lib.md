# Example: sway_sources/sway/docs/reference/src/code/language/program-types/libraries/external/my_library/src/lib.sw

```sway
library;

use my_other_library::quix;

// `quix` from `my_other_library` is now available throughout the file

```
