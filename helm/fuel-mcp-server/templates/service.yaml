apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuel-mcp-server.fullname" . }}
  labels:
    {{- include "fuel-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuel-mcp-server.selectorLabels" . | nindent 4 }}
